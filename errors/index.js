const notFoundError = require('./notFound.error'),
      notAuthError = require('./notAuth.error'),
      notValidError = require('./notValid.error')

module.exports = {
  notFoundError,
  notAuthError,
  notValidError
}